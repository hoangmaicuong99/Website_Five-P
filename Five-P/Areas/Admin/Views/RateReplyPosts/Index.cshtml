@using Five_P.Models;
@model List<Rate_Reply_Post>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    int intCount = 1;
}
<div class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title"> Đánh giá trả lời bài viết ( @ViewBag.SumRateRP )</h4>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table">
                            <thead class=" text-primary">
                            <th>
                                Stt
                            </th>
                            <th>
                                <i class="fas fa-reply-all"></i> Trả lời
                            </th>
                            <th class="text-center">
                                <i class="fas fa-medal"></i> Đánh giá
                            </th>
                            <th class="text-center">
                                <i class="now-ui-icons users_single-02"></i> Người dùng
                            </th>
                            <th class="text-center">
                                <i class="fas fa-magic"></i> Tùy chọn
                            </th>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @intCount
                                        </td>
                                        <td>
                                            @item.Reply_Post.reply_post_title
                                        </td>
                                        <td class="text-center">
                                            @if (item.rate_reply_post1 == true)
                                            {
                                                <i class="far fa-thumbs-up"></i>
                                            }
                                            else
                                            {
                                                <i class="far fa-thumbs-down"></i>
                                            }
                                        </td>
                                        <td class="text-center">
                                            @item.User.user_nicename
                                        </td>
                                        <td class="text-center">
                                            <i class="fas fa-user-edit"></i> |
                                            <a href="/Home/DetailQuestion?idpost=@item.Reply_Post.Post.post_id"><i class="far fa-eye"></i></a> |
                                            <i class="far fa-trash-alt"></i>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

